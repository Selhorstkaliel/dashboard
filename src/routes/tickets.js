const express = require('express');
const router = express.Router();

// Placeholder for tickets routes
router.get('/', (req, res) => {
  res.json({ message: 'Tickets API - Em desenvolvimento' });
});

module.exports = router;
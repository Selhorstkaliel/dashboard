const express = require('express');
const router = express.Router();

// Placeholder for validate routes
router.get('/', (req, res) => {
  res.json({ message: 'Validate API - Em desenvolvimento' });
});

module.exports = router;
const express = require('express');
const router = express.Router();

// Placeholder for charts routes
router.get('/', (req, res) => {
  res.json({ message: 'Charts API - Em desenvolvimento' });
});

module.exports = router;
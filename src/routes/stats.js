const express = require('express');
const router = express.Router();

// Placeholder for stats routes
router.get('/', (req, res) => {
  res.json({ message: 'Stats API - Em desenvolvimento' });
});

module.exports = router;
const express = require('express');
const router = express.Router();

// Placeholder for entries routes
router.get('/', (req, res) => {
  res.json({ message: 'Entries API - Em desenvolvimento' });
});

module.exports = router;
const express = require('express');
const router = express.Router();

// Placeholder for scheduler routes
router.get('/', (req, res) => {
  res.json({ message: 'Scheduler API - Em desenvolvimento' });
});

module.exports = router;